{% extends "::base.html.twig" %}

{% trans_default_domain 'At21EBoxOfficeBundle' %}

{% block body %}
    <div class="sixteen wide column">
        <form class="ui form" action="{{ path('at21_eboxoffice_admin_new_event') }}" {{ form_enctype(form) }} method="POST" class="fos_user_profile_edit">
            <h4 class="ui dividing header">New Event</h4>
            <div class="four fields">
                <div class="ui input field">
                    {{ form_label(form.date) }}
                    {{ form_widget(form.date) }}
                </div>
                <div class="ui input field">
                    {{ form_label(form.title) }}
                    {{ form_widget(form.title) }}
                </div>
                {{ form_widget(form._token) }}
            </div>
            <div>
                <input class="ui submit button" type="submit" value="{{ 'admin.new_event.submit'|trans }}" />
            </div>
        </form>
    </div>
{% endblock %}